# AI Stock Analysis Agent

A Python-based AI agent that fetches stock data, analyzes key metrics,
applies heuristic-based prediction logic, and generates automated reports.

## Run
pip install -r requirements.txt
python main.py
